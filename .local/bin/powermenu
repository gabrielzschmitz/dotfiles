#!/bin/sh
#         .             .              .		  
#         |             |              |           .	  
# ,-. ,-. |-. ,-. . ,-. |  ,_, ,-. ,-. |-. ,-,-. . |- ,_, 
# | | ,-| | | |   | |-' |   /  `-. |   | | | | | | |   /  
# `-| `-^ ^-' '   ' `-' `' '"' `-' `-' ' ' ' ' ' ' `' '"' 
#  ,|							  
#  `'							  
# GITHUB:https://github.com/gabrielzschmitz		  
# INSTAGRAM:https://www.instagram.com/gabrielz.schmitz/   
# DOTFILES:https://github.com/gabrielzschmitz/dotfiles/   

# System management menu using dmenu

# Options
shutdown="⏻  Shutdown"
reboot="  Restart "
logout="  Logout  "
suspend="⏾  Sleep   "
lock="  Lock    "

# Variables
options="$shutdown\n$reboot\n$logout\n$suspend\n$lock"

# Choices
chosen="$(printf "$options" | dmenucenter -l 5)"
case $chosen in
    "$shutdown")
		herbe "⏻ Turning off the machine..." &
                sleep 1  && loginctl poweroff
        ;;
    "$reboot")
		herbe " Restarting the machine..." &
                sleep 1  && loginctl reboot
	;;
    "$logout")
		herbe " Login out of the system..." &
                sleep 1  && loginctl terminate-session $XDG_SESSION_ID
	;;
    "$suspend")
		herbe "⏾ Putting the system to sleep..." &
                sleep 1  && loginctl suspend
        ;;
    "$lock")
		herbe " Locking the system..." &
                sleep 5.1 && slock
        ;;
esac

