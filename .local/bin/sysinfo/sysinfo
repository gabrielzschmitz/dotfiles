#!/bin/sh
#         .             .              .		  
#         |             |              |           .	  
# ,-. ,-. |-. ,-. . ,-. |  ,_, ,-. ,-. |-. ,-,-. . |- ,_, 
# | | ,-| | | |   | |-' |   /  `-. |   | | | | | | |   /  
# `-| `-^ ^-' '   ' `-' `' '"' `-' `-' ' ' ' ' ' ' `' '"' 
#  ,|							  
#  `'							  
# GITHUB:https://github.com/gabrielzschmitz		  
# INSTAGRAM:https://www.instagram.com/gabrielz.schmitz/   
# DOTFILES:https://github.com/gabrielzschmitz/dotfiles/   

# System informations menu

# Options
uptime=" $(sysinfo-uptime)"
internet="$(sysinfo-internet)"
dates="$(sysinfo-date)"
memory="$(sysinfo-memory)"
weather="$(sysinfo-weather)"
packages="$(sysinfo-packages)"
disks="$(sysinfo-disks)"
battery="$(sysinfo-batteryicon)$(sysinfo-battery)"
manual="  Rice manual"

# Variables
options="$uptime\n$internet\n$dates\n$memory\n$weather\n$packages\n$disks\n$battery\n$manual"

# Choices
chosen="$(echo -e "$options" | dmenucenter -l 10)"
case $chosen in
    "$uptime")
	    	notify-send " Uptime now:" "$(sysinfo-uptime)"
	;;
    "$internet")
	    	notify-send " Network Manager:" &
                nmdmenu
        ;;
    "$dates")
	    	notify-send " This month:" "$(sysinfo-calendar)"
        ;;
    "$memory")
	    	notify-send " Most intences in memory:" "$(sysinfo-mostinmemory)"
        ;;
    "$weather")
	    	notify-send " The weather today:" &
                setsid -f "st" -c weatherreport -e less -Srf "${XDG_DATA_HOME:-$HOME/.local/share}/weatherreport"
        ;;
    "$packages")
	    	notify-send " Installed packages:" "$(sysinfo-packages)" &
                setsid -f "st" -c packagesupgrade -e sysinfo-packagesupgrade
        ;;
    "$disks")
	    	notify-send " Disks:" "$(lsblk --output NAME,SIZE,TYPE)"
        ;;
    "$battery")
	    	notify-send "$(sysinfo-batteryicon) Battery now is:" "$(sysinfo-battery)"
        ;;
    "$manual")
	    	notify-send " This is your manual to my rice!" &
                groff -mom $HOME/.local/share/dwm/gzdots.mom -T pdf | zathura -
esac

