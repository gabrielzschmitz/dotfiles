[module/date]
type = internal/date
interval = 1

time = " %H:%M"

format = <label>
format-background = ${colors.bg}
format-foreground = ${colors.fg}
format-padding = 1
label = %{A1:notify-send "$(sysinfo-calendar)":}%time%%{A}
