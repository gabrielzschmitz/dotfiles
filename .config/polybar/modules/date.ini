[module/date]
type = internal/date
interval = 1

time = "%H:%M"

format = <label>
format-background = ${colors.bg}
format-foreground = ${colors.white}
format-padding = 1
label = %{A1:notify-send -t 0 "This Month" "$(cal)":}%time%%{A}
